# =============================================================
# BigQuery Schema Migrator — Environment Configuration
# =============================================================
# Copy this file to .env and fill in your values.
# The .env file is listed in .gitignore and will never be committed.
#
# Usage:
#   cp .env.example .env
#   # edit .env with your values, then:
#   python migrate.py --status
# =============================================================

# ----------------------------------------------------
# GCP Authentication — Service Account Key
#
# Option A (recommended): Drop any .json key file into the credentials/ folder.
#   The script will auto-discover it regardless of the filename.
#   credentials/ is listed in .gitignore and is safe from commits.
#
# Option B: Set the path explicitly below (overrides auto-discovery).
#   Download your key at: GCP Console → IAM → Service Accounts → Keys
# ----------------------------------------------------
# GOOGLE_APPLICATION_CREDENTIALS=credentials/your-service-account.json

# ----------------------------------------------------
# GCP Project & Dataset
# Used by migrate.py.
# CLI args (--project, --dataset) override these if provided.
# ----------------------------------------------------
GCP_PROJECT_ID=your-gcp-project-id
GCP_DATASET_ID=your-bigquery-dataset-id

# ----------------------------------------------------
# BigQuery Location
# Region where your dataset lives.
# Examples: US, EU, southamerica-east1, us-central1, us-east1
# Default: US
# ----------------------------------------------------
GCP_LOCATION=US
