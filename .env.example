# =============================================================
# BigQuery Schema Migrator — Environment Configuration
# =============================================================
# Copy this file to .env and fill in your values.
# The .env file is listed in .gitignore and will never be committed.
#
# Usage:
#   cp .env.example .env
#   # edit .env with your values
#   python run_migrations.py
# =============================================================

# ----------------------------------------------------
# GCP Authentication
# Path to the Service Account JSON key file.
# Download from: GCP Console → IAM → Service Accounts → Keys
# ----------------------------------------------------
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your-service-account.json

# ----------------------------------------------------
# GCP Project & Dataset
# These are used by both run_migrations.py and deploy_schedules.py.
# CLI args (--project-id, --dataset-id) override these if provided.
# ----------------------------------------------------
GCP_PROJECT_ID=your-gcp-project-id
GCP_DATASET_ID=your-bigquery-dataset-id

# ----------------------------------------------------
# BigQuery Location
# Region where your dataset lives.
# Examples: US, EU, southamerica-east1, us-central1
# Used by: deploy_schedules.py
# Default: US
# ----------------------------------------------------
GCP_LOCATION=US

# ----------------------------------------------------
# Scheduled Query — Schedule Expression
# How often the scheduled queries should run.
# Examples: "every 1 hours", "every 24 hours", "every monday 09:00"
# Used by: deploy_schedules.py
# Default: every 1 hours
# ----------------------------------------------------
GCP_SCHEDULE=every 1 hours
